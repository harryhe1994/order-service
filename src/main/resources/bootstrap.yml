server:
  port: 9082

spring:
  application:
    name: order-service
  #TODO: add configs for config server
  cloud:
      config:
        discovery:
          enabled: true
          serviceId: config-server
        fail-fast: true
        enabled: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://eureka-server:8080/eureka/
